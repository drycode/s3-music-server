const PlayQueue = require("../../src/models/playqueue");

let queue = PlayQueue();

describe("Test PlayQueue", () => {
  beforeEach(() => {
    queue = PlayQueue();
    queue.enque;
  });
  describe("enqueue", () => {});
  describe("dequeue", () => {});
  describe("remove", () => {});
  describe("move", () => {});
  describe("#insert", () => {});
});
